spring:
  cloud:
    config:
      server:
        native:
          search-locations: file:D:/Mayur/claimsassist/insurance-ai-platform/config-repo

server:
  port: 8888

logging:
  level:
    com.claimassist: DEBUG
    org.springframework.cloud.config: DEBUG

