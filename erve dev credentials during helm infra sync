[1mdiff --git a/.github/workflows/cd.yml b/.github/workflows/cd.yml[m
[1mindex 72e5c5f..f8c6a9a 100644[m
[1m--- a/.github/workflows/cd.yml[m
[1m+++ b/.github/workflows/cd.yml[m
[36m@@ -449,8 +449,26 @@[m [mjobs:[m
             --set image.registry="$DOCKER_REGISTRY" > /dev/null[m
       - name: Apply infrastructure change via Helm (DEV)[m
         if: needs.resolve-targets.outputs.force_deploy != 'true'[m
[32m+[m[32m        env:[m
[32m+[m[32m          DEV_POSTGRES_USER: ${{ secrets.DEV_POSTGRES_USER }}[m
[32m+[m[32m          DEV_POSTGRES_PASSWORD: ${{ secrets.DEV_POSTGRES_PASSWORD }}[m
[32m+[m[32m          DEV_KEYCLOAK_ADMIN_PASSWORD: ${{ secrets.DEV_KEYCLOAK_ADMIN_PASSWORD }}[m
[32m+[m[32m          DEV_KEYCLOAK_ADMIN_CLIENT_SECRET: ${{ secrets.DEV_KEYCLOAK_ADMIN_CLIENT_SECRET }}[m
[32m+[m[32m          DEV_KEYCLOAK_DB_USER: ${{ secrets.DEV_KEYCLOAK_DB_USER }}[m
[32m+[m[32m          DEV_KEYCLOAK_DB_PASSWORD: ${{ secrets.DEV_KEYCLOAK_DB_PASSWORD }}[m
         run: |[m
           set -euo pipefail[m
[32m+[m[32m          # Fail fast if any required DEV credential is missing. A normal infra sync[m
[32m+[m[32m          # can overwrite Kubernetes Secrets with empty values if these are not[m
[32m+[m[32m          # explicitly passed to the Helm upgrade.[m
[32m+[m[32m          for v in DEV_POSTGRES_USER DEV_POSTGRES_PASSWORD \[m
[32m+[m[32m                    DEV_KEYCLOAK_ADMIN_PASSWORD DEV_KEYCLOAK_ADMIN_CLIENT_SECRET \[m
[32m+[m[32m                    DEV_KEYCLOAK_DB_USER DEV_KEYCLOAK_DB_PASSWORD; do[m
[32m+[m[32m            if [ -z "${!v:-}" ]; then[m
[32m+[m[32m              echo "::error::$v (GitHub secret, dev environment) is required for DEV infrastructure sync."[m
[32m+[m[32m              exit 1[m
[32m+[m[32m            fi[m
[32m+[m[32m          done[m
           # No image.tag / imageTag overrides -> helm re-renders the whole chart[m
           # from saved release values, applying the changed chart/values, while[m
           # every application keeps its already-deployed SHA (no rollout).[m
[36m@@ -461,6 +479,13 @@[m [mjobs:[m
             --create-namespace \[m
             -f infrastructure/helm/claimassist/values-dev.yaml \[m
             --set image.registry="$DOCKER_REGISTRY" \[m
[32m+[m[32m            --set postgresql.credentials.postgresUsername="$DEV_POSTGRES_USER" \[m
[32m+[m[32m            --set postgresql.credentials.postgresPassword="$DEV_POSTGRES_PASSWORD" \[m
[32m+[m[32m            --set keycloak.credentials.adminUsername="admin" \[m
[32m+[m[32m            --set keycloak.credentials.adminPassword="$DEV_KEYCLOAK_ADMIN_PASSWORD" \[m
[32m+[m[32m            --set keycloak.credentials.adminClientSecret="$DEV_KEYCLOAK_ADMIN_CLIENT_SECRET" \[m
[32m+[m[32m            --set keycloak.postgresql.credentials.username="$DEV_KEYCLOAK_DB_USER" \[m
[32m+[m[32m            --set keycloak.postgresql.credentials.password="$DEV_KEYCLOAK_DB_PASSWORD" \[m
             --wait \[m
             --timeout 10m[m
       - name: Bootstrap DEV environment (full install, all services at SHA)[m
