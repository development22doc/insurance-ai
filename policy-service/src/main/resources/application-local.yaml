# LOCAL profile: run policy-service locally
spring:
  config:
    import: optional:file:../application-local.properties

server:
  port: 8084

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/claimassist_policy
    username: ${POLICY_DB_USER:policy}
    password: ${POLICY_DB_PASSWORD:policy}

# No DB access in Phase 1 (scaffold).
