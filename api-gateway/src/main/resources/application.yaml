spring:
  application:
    name: api-gateway

  config:
    # Make config server optional - use local defaults if unavailable
    import: optional:configserver:${CONFIG_SERVER_URL:http://localhost:8888}
